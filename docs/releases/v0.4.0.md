# CrealityOS v0.4.0

## Summary

CrealityOS v0.4.0 preserves the current v0.3.0 functionality as a new non-overwriting version checkpoint. It keeps the PSD production skill, artifact discovery improvements, designer review packet improvements, Meegle writeback artifact selection, and workflow fallback guidance from the current code line while advancing version metadata to `0.4.0`.

## Highlights

- Saved the current CrealityOS code line as a new `v0.4.0` version.
- Preserved previous release tags including `v0.2.0` and `v0.3.0`.
- Updated package metadata from `0.3.0` to `0.4.0`.
- Updated Workbench metadata from `0.3` to `0.4`.
- Kept `game-asset-psd-breakdown` as the production-oriented PSD/image-to-PSD skill.

## Validation

- Version metadata should report `0.4.0` from both `pyproject.toml` and `agent.__version__`.
- Existing unit tests should remain green before publishing the tag.

## Notes

- Do not force-push or overwrite earlier version tags.
- Publish `v0.4.0` as a new tag after committing this version checkpoint.
