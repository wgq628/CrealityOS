# 风格记忆治理 - {{project_key}}

- 同品类底座：`{{same_category}}`
- 模式：`{{mode}}`

## 建议/动作

- `promote_k3_to_k2`：多次证据支持的 K3 可升级为 K2。
- `sync_confirmed_avoid_to_do_not`：已确认的否定约束同步进入 `do_not`。
- `keyword_conflict`：同一关键词同时存在于视觉关键词和禁忌项，需要设计师裁决。

## 警告

- 单证据 K3 保持待验证。
- 关键词冲突不自动应用。

## 下一步

- 报告模式不会修改记忆；确认后用 `--apply` 写回安全动作。
- 治理后重新生成创作包，确认 K1/K2 进入执行提示词，K3 仍留在不确定项。
